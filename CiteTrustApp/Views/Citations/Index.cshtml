@model IEnumerable<CiteTrustApp.Models.Citation>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_LayoutAd.cshtml";
}

<h2>Trích dẫn</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table datatable">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Evidence.Content)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FormatType)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CitationText)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Evidence.Content)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FormatType)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CitationText)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </td>
            </tr>
        }
        </tbody>
</table>
